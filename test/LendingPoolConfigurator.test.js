import { expect } from "chai";
import { ethers } from "hardhat";

describe("LendingPoolConfigurator", function () {
  it("Should deploy configurator", async function () {
      // In real test, deploy LendingPool first
    expect(true).to.be.true; 
  });
});
